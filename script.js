/* ── SHARED FOOTER: injected on every page from one template here ──
   To edit the footer, edit SITE_FOOTER_HTML below — it updates every
   page automatically. Each page just needs an empty
   <div id="site-footer"></div> where the footer should appear. ── */
(function () {
  var SITE_FOOTER_HTML = `<section>
  <div class="pre-footer" id="1786948733212" bis_skin_checked="1"> 
      <div class="pre-footer-inner" bis_skin_checked="1"> 
        <div class="pf-item" bis_skin_checked="1"> 
          <div class="pf-title" bis_skin_checked="1">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> 
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"> 
              </path> 
            </svg> Certified Training 
          </div> 
          <p>Every course is led by professionally certified nail educators with real salon experience. 
          </p> 
        </div> 
        <div class="pf-item" bis_skin_checked="1"> 
          <div class="pf-title" bis_skin_checked="1">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> 
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"> 
              </path> 
            </svg> Always Here for You 
          </div> 
          <p>Call or WhatsApp us anytime<br>+91 88791 92901<br>We respond within a few hours, always. 
          </p> 
        </div> 
        <div class="pf-item" bis_skin_checked="1"> 
          <div class="pf-title" bis_skin_checked="1">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> 
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"> 
              </path> 
            </svg> Bulk &amp; Salon Enquiries 
          </div> 
          <p>Training your team or setting up a salon? Reach us at <a href="mailto:business@ellementco.com" style="color:inherit; text-decoration:underline;">business@ellementco.com</a>
          </p> 
        </div> 
        <div class="pf-item" bis_skin_checked="1"> 
          <div class="pf-title" bis_skin_checked="1">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> 
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"> 
              </path> 
              <circle cx="12" cy="10" r="3"> 
              </circle> 
            </svg> Visit Our Academy 
          </div> 
          <p>503, Wellington Business Park 1, near Mittal Industrial Estate, Marol Naka, Andheri East, Mumbai - 400059 
          </p> 
        </div> 
      </div>
      </div><footer class="footer-wrapper new-footer">
    <div class="footer-wave" bis_skin_checked="1">
      <svg viewBox="0 0 1440 100" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M0,0 L0,20 C 300,90 550,80 850,35 C 1150,-10 1350,20 1440,90 L1440,0 Z" class="wave-fill"></path>
        <path d="M0,25 C 300,95 550,85 850,40 C 1150,-5 1350,25 1440,95" class="wave-stroke"></path>
      </svg>
    </div>
    <div class="footer-newsletter-block" bis_skin_checked="1">
      <svg class="logo-svg footer-logo-svg" style="height:60px;width:auto;display:block;margin:0 auto 8px;" version="1.0" xmlns="http://www.w3.org/2000/svg"
 viewBox="136 341 1343 401"
 preserveAspectRatio="xMidYMid meet" role="img" aria-label="Ellement Co. Nail Academy">
<g transform="translate(0.000000,1128.000000) scale(0.100000,-0.100000)"
fill="#000000" stroke="none">
<path d="M14119 7618 c-8 -27 -5 -38 11 -38 46 0 50 -8 50 -101 0 -82 1 -89
20 -89 19 0 20 7 20 90 l0 90 30 0 c25 0 30 4 30 25 0 25 -1 25 -80 25 -44 0
-80 -1 -81 -2z"/>
<path d="M14300 7505 c0 -107 1 -115 20 -115 17 0 19 8 22 73 l3 72 24 -72
c17 -51 30 -73 41 -73 11 0 25 23 43 73 l26 72 0 -72 c1 -66 3 -73 21 -73 19
0 20 7 20 115 0 114 0 115 -24 115 -30 0 -40 -13 -62 -82 -10 -32 -21 -58 -24
-58 -3 0 -14 26 -24 58 -22 69 -32 82 -62 82 -24 0 -24 -1 -24 -115z"/>
<path d="M2319 7137 c-17 -22 -20 -22 -360 -27 l-344 -5 0 -35 c0 -33 2 -35
388 6 6 33 15 60 18 42 4 47 8 50 33 l3 27 -241 0 c-205 0 -241 -2 -246 -15z"/>
<path d="M4766 5105 c-11 -30 5 -45 49 -45 30 0 47 -6 61 -21 18 -20 19 -43
22 -353 2 -182 0 -350 -3 -374 -8 -57 -34 -82 -84 -82 -40 0 -56 -16 -45 -45
5 -12 65 -15 399 -17 l394 -3 28 -27 c54 -52 57 -46 82 155 12 100 25 199 28
220 5 36 4 38 -16 35 -17 -2 -31 -24 -63 -95 -43 -100 -104 -178 -153 -199
-30 -12 -98 -16 -263 -15 l-83 1 3 390 c3 447 -2 421 96 430 53 5 57 7 60 33
l3 27 -254 0 c-217 0 -256 -2 -261 -15z"/>
<path d="M9684 5105 c-12 -31 4 -45 50 -45 80 0 80 -2 84 -370 5 -437 3 -449
-84 -459 -44 -5 -49 -9 -49 -31 l0 -25 305 0 c345 0 403 8 530 71 172 86 259
239 247 432 -13 214 -172 369 -431 423 -123 25 -643 29 -652 4z m521 -55 c140
-21 240 -91 291 -204 49 -109 45 -315 -8 -419 -32 -64 -93 -122 -161 -153 -53
-24 -78 -29 -168 -32 -104 -4 -106 -4 -112 19 -9 31 -9 727 -1 768 6 29 9 31
48 31 22 0 72 -5 111 -10z"/>
<path d="M12020 5091 c0 -27 3 -29 54 -35 95 -12 91 7 91 -411 0 -419 5 -396
-96 -417 -51 -10 -59 -15 -59 -35 l0 -23 215 0 215 0 0 29 c0 27 -2 28 -58 34
-72 6 -116 27 -131 64 -8 20 -11 126 -9 360 l3 332 184 -295 c101 -162 186
-294 190 -294 3 0 31 40 60 88 50 79 259 384 303 441 18 23 18 16 18 -318 0
-328 -1 -342 -20 -361 -14 -14 -33 -20 -65 -20 -43 0 -45 -1 -45 -30 l0 -30
243 2 242 3 3 28 c3 25 0 27 -30 27 -52 0 -75 10 -87 36 -18 39 -15 744 3 769
9 12 31 21 62 24 44 5 49 9 52 34 l3 27 -163 0 -163 -1 -165 -238 c-91 -131
-167 -237 -170 -236 -3 2 -70 109 -150 239 l-145 236 -192 0 -193 0 0 -29z"/>
<path d="M13472 5093 c3 -25 8 -29 52 -34 43 -5 55 -11 90 -51 23 -25 98 -149
169 -276 l127 -231 0 -89 c0 -97 -12 -147 -40 -162 -9 -5 -47 -12 -83 -16 -66
-7 -83 -18 -71 -49 5 -13 46 -15 308 -13 l301 3 0 25 c0 23 -5 25 -75 35 -41
6 -80 14 -87 18 -20 13 -33 77 -33 170 l0 88 120 238 c129 257 157 295 227
306 59 10 63 13 63 40 l0 25 -185 0 -185 0 0 -30 c0 -28 3 -30 34 -30 19 0 46
-4 60 -10 46 -17 35 -59 -70 -271 -85 -172 -96 -190 -108 -173 -7 10 -54 99
-104 197 -64 123 -92 188 -92 212 0 38 7 41 85 48 35 2 40 6 43 30 l3 27 -276
0 -276 0 3 -27z"/>
</g>
</svg>
      <p class="footer-newsletter-tagline">India's premium nail brand. Vegan, cruelty-free, made for every skin tone. Your nails, your Ellement era!</p>
      <h3 class="footer-newsletter-title">Stay in the loop</h3>
      <p class="footer-newsletter-sub">New shades, tutorials, exclusive offers.</p>
      <div class="subscribe-form" bis_skin_checked="1">
        <input type="email" placeholder="Your email address">
        <button>Subscribe</button>
      </div>
      <span class="footer-copy" style="display: flex; align-items: center; gap: 14px; flex-wrap: wrap; justify-content: center;">
        <a href="https://chat.whatsapp.com/HyUikRkMOMa0UhP9M2Wnu3" target="_blank" rel="noopener" style="color: #000 !important; display: flex; align-items: center; gap: 5px; text-decoration: underline !important;">Open this link to join my WhatsApp Community <img src="https://cdn.shopify.com/s/files/1/0711/5725/2400/files/whatsapp_community.png?v=1789460830" alt="WhatsApp Community" style="max-height: 18px;"></a>
        <a href="https://youtube.com/@ellementco.nailacademy?si=M7hqMnNBTlMI-9uC" target="_blank" rel="noopener" style="color: #40314e !important; display: flex; align-items: center; gap: 6px; text-decoration: underline !important;">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="#E0C7D6" stroke="#40314e" stroke-width="0.6"><path d="M22.54 6.42a2.78 2.78 0 0 0-1.95-1.95C18.88 4 12 4 12 4s-6.88 0-8.59.46a2.78 2.78 0 0 0-1.95 1.95C1 8.12 1 12 1 12s0 3.88.46 5.58a2.78 2.78 0 0 0 1.95 1.95C5.12 20 12 20 12 20s6.88 0 8.59-.46a2.78 2.78 0 0 0 1.95-1.95C23 15.88 23 12 23 12s0-3.88-.46-5.58z"></path><polygon points="9.75 15.02 15.5 12 9.75 8.98 9.75 15.02" fill="#40314e" stroke="none"></polygon></svg>
          @ellementco.nailacademy
        </a>
      </span>
    </div>
    <div class="footer-bottom" bis_skin_checked="1">
      <span class="footer-copy">© 2026 Ellement Co. Nail Academy. All rights reserved.</span>
    </div>
  </footer>
  </section>`;
  function injectFooter() {
    var mount = document.getElementById('site-footer');
    if (!mount) return;
    mount.outerHTML = SITE_FOOTER_HTML;
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', injectFooter);
  } else {
    injectFooter();
  }
})();

/* ── REDIRECT TO THANK YOU PAGE AFTER CONSULTATION FORM SUBMISSION ──
   Graphy's .spContactForm submits via its own platform script (not this
   file), posting to an endpoint containing "enquiry". Rather than guess
   at DOM success markers, we patch fetch() and XMLHttpRequest to watch
   for that request completing successfully, then redirect. ── */
(function () {
  var THANK_YOU_URL = 'https://ellementconailacademy.graphy.com/s/pages/thank-you';
  var redirected = false;

  function goToThankYou() {
    if (redirected) return;
    redirected = true;
    window.location.href = THANK_YOU_URL;
  }

  function looksLikeEnquiry(url) {
    return typeof url === 'string' && /enquiry/i.test(url);
  }

  // Patch fetch
  var origFetch = window.fetch;
  if (origFetch) {
    window.fetch = function (input, init) {
      var url = typeof input === 'string' ? input : (input && input.url);
      return origFetch.apply(this, arguments).then(function (response) {
        if (looksLikeEnquiry(url) && response && response.ok) {
          setTimeout(goToThankYou, 400);
        }
        return response;
      });
    };
  }

  // Patch XMLHttpRequest (in case Graphy's form uses XHR instead of fetch)
  var origOpen = XMLHttpRequest.prototype.open;
  XMLHttpRequest.prototype.open = function (method, url) {
    this.__isEnquiry = looksLikeEnquiry(url);
    return origOpen.apply(this, arguments);
  };
  var origSend = XMLHttpRequest.prototype.send;
  XMLHttpRequest.prototype.send = function () {
    if (this.__isEnquiry) {
      this.addEventListener('load', function () {
        if (this.status >= 200 && this.status < 300) {
          setTimeout(goToThankYou, 400);
        }
      });
    }
    return origSend.apply(this, arguments);
  };
})();

(function () {
  function pinFooterToEnd() {
    var footer = document.querySelector('.footer-new-at-last');
    if (!footer) return;
    if (document.body.lastElementChild !== footer) {
      document.body.appendChild(footer);
    }
  }
  function start() {
    pinFooterToEnd();
    var observer = new MutationObserver(function (mutations) {
      for (var i = 0; i < mutations.length; i++) {
        if (mutations[i].addedNodes.length) {
          pinFooterToEnd();
          break;
        }
      }
    });
    observer.observe(document.body, { childList: true });
  }
  if (document.body) {
    start();
  } else {
    document.addEventListener('DOMContentLoaded', start);
  }
  window.addEventListener('load', pinFooterToEnd);
})();

(function () {
  function init() {
    (function () {
      document.addEventListener('click', function (e) {
        var trigger = e.target.closest ? e.target.closest('.faq-trigger') : null;
        if (!trigger) return;
        var item = trigger.closest('.faq-item');
        if (!item) return;
        var answer = item.querySelector('.faq-answer');
        if (!answer) return;
        var isOpen = item.classList.contains('is-open');
        if (isOpen) {
          item.classList.remove('is-open');
          answer.style.setProperty('display', 'none', 'important');
        } else {
          item.classList.add('is-open');
          answer.style.setProperty('display', 'block', 'important');
        }
      }, true);
    })();
    /* ── LEVEL BOXES: pair-match heights (Level 1↔4, Level 2↔3) ── */
    (function () {
      const grid = document.querySelector('.level-grid');
      if (!grid) return;
      const boxes = Array.from(grid.children);
      if (boxes.length < 4) return;
      function matchHeights() {
        boxes.forEach(b => { b.style.height = 'auto'; });
        if (window.innerWidth <= 700) return;
        const pairs = [[0, 3], [1, 2]];
        pairs.forEach(([a, b]) => {
          const h = Math.max(boxes[a].offsetHeight, boxes[b].offsetHeight);
          boxes[a].style.height = h + 'px';
          boxes[b].style.height = h + 'px';
        });
      }
      window.addEventListener('load', matchHeights);
      window.addEventListener('resize', matchHeights);
      matchHeights();
      setTimeout(matchHeights, 300);
    })();
    /* ── CONSULTATION FORM: mailto with field data ── */
    (function () {
      var btn = document.getElementById('consult-submit');
      if (!btn) return;
      btn.addEventListener('click', function () {
        var v = function (id) { var el = document.getElementById(id); return el ? el.value : ''; };
        var body = 'Name: ' + v('consult-name') +
          '\nPhone: ' + v('consult-phone') +
          '\nEmail: ' + v('consult-email') +
          '\nPlace: ' + v('consult-place') +
          '\nMessage: ' + v('consult-message');
        window.location.href = 'mailto:Academy@ellementco.com' +
          '?subject=' + encodeURIComponent('Free Consultation Request') +
          '&body=' + encodeURIComponent(body);
      });
    })();
    /* ── HERO CAROUSEL ── */
    (function () {
      const carousel = document.getElementById('heroCarousel');
      if (!carousel) return;
      const slides = Array.from(carousel.querySelectorAll('.hero-slide'));
      const dotsWrap = document.getElementById('heroDots');
      const total = slides.length;
      let current = 0, autoplayTimer = null;
      const AUTOPLAY_MS = 4000;
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      dotsWrap.innerHTML = '';
      slides.forEach((_, i) => {
        const d = document.createElement('button');
        d.className = 'dot';
        d.setAttribute('aria-label', 'Slide ' + (i + 1));
        d.addEventListener('click', () => { goTo(i); restartAutoplay(); });
        dotsWrap.appendChild(d);
      });
      const dots = Array.from(dotsWrap.children);
      function render() {
        slides.forEach((slide, i) => {
          slide.classList.remove('is-active','is-prev','is-next','is-far-prev','is-far-next','is-hidden');
          const prev = (current - 1 + total) % total;
          const next = (current + 1) % total;
          const farPrev = (current - 2 + total) % total;
          const farNext = (current + 2) % total;
          if (i === current) slide.classList.add('is-active');
          else if (i === prev) slide.classList.add('is-prev');
          else if (i === next) slide.classList.add('is-next');
          else if (total > 4 && i === farPrev) slide.classList.add('is-far-prev');
          else if (total > 4 && i === farNext) slide.classList.add('is-far-next');
          else slide.classList.add('is-hidden');
          const v = slide.querySelector('video');
          if (v) {
            if (i === current) { v.play().catch(()=>{}); }
            else {
              v.pause(); v.muted = true;
              const b = slide.querySelector('.sound-btn');
              if (b) {
                const p = b.querySelector('.icon-play'), vol = b.querySelector('.icon-volume');
                if (p) p.style.display = '';
                if (vol) vol.style.display = 'none';
                b.classList.remove('faded');
              }
            }
          }
        });
        dots.forEach((d, i) => d.classList.toggle('active', i === current));
      }
      function goTo(i) { current = (i + total) % total; render(); }
      function startAutoplay() { if (!reduced) autoplayTimer = setInterval(() => goTo(current + 1), AUTOPLAY_MS); }
      function restartAutoplay() { clearInterval(autoplayTimer); startAutoplay(); }
      slides.forEach(slide => {
        slide.addEventListener('click', () => {
          if (slide.classList.contains('is-prev')) { goTo(current - 1); restartAutoplay(); }
          else if (slide.classList.contains('is-next')) { goTo(current + 1); restartAutoplay(); }
          else if (slide.classList.contains('is-far-prev')) { goTo(current - 2); restartAutoplay(); }
          else if (slide.classList.contains('is-far-next')) { goTo(current + 2); restartAutoplay(); }
        });
      });
      let startX = null;
      carousel.addEventListener('pointerdown', e => { startX = e.clientX; });
      window.addEventListener('pointerup', e => {
        if (startX === null) return;
        const dx = e.clientX - startX;
        if (Math.abs(dx) > 40) { dx < 0 ? goTo(current+1) : goTo(current-1); restartAutoplay(); }
        startX = null;
      });
      let wheelLock = false;
      carousel.addEventListener('wheel', e => {
        if (Math.abs(e.deltaX) <= Math.abs(e.deltaY)) return;
        e.preventDefault();
        if (wheelLock) return;
        wheelLock = true;
        e.deltaX > 0 ? goTo(current+1) : goTo(current-1);
        restartAutoplay();
        setTimeout(() => { wheelLock = false; }, 500);
      }, { passive: false });
      carousel.addEventListener('mouseenter', () => clearInterval(autoplayTimer));
      carousel.addEventListener('mouseleave', restartAutoplay);
      render(); startAutoplay();
    })();
    /* ── HERO VIDEO SOUND TOGGLE ── */
    (function() {
      var fadeTimers = new WeakMap();
      function startFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        var t = setTimeout(function() { btn.classList.add('faded'); }, 1000);
        fadeTimers.set(btn, t);
      }
      function cancelFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        btn.classList.remove('faded');
      }
      document.addEventListener('click', function(e) {
        var btn = e.target.closest ? e.target.closest('.hero-slide .sound-btn') : null;
        if (!btn) return;
        e.preventDefault(); e.stopPropagation();
        var box = btn.closest('.hero-slide');
        if (!box) return;
        var vid = box.querySelector('.hero-video');
        if (!vid) return;
        var wasMuted = vid.muted;
        document.querySelectorAll('.hero-slide').forEach(function(b) {
          var v2  = b.querySelector('.hero-video');
          var b2  = b.querySelector('.sound-btn');
          if (!v2 || !b2) return;
          v2.muted = true;
          b2.querySelector('.icon-play').style.display   = '';
          b2.querySelector('.icon-volume').style.display = 'none';
          cancelFade(b2);
        });
        if (wasMuted) {
          vid.muted = false;
          btn.querySelector('.icon-play').style.display   = 'none';
          btn.querySelector('.icon-volume').style.display = '';
          startFade(btn);
        }
      }, true);
      document.addEventListener('mouseover', function(e) {
        var box = e.target.closest ? e.target.closest('.hero-slide') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        if (!btn) return;
        cancelFade(btn);
      }, true);
      document.addEventListener('mouseout', function(e) {
        var box = e.target.closest ? e.target.closest('.hero-slide') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        var vid = box.querySelector('.hero-video');
        if (!btn || !vid) return;
        if (!vid.muted) startFade(btn);
      }, true);
    })();
    /* ── FOUNDER VIDEO SOUND TOGGLE ── */
    (function() {
      var video = document.querySelector('.founder-video');
      if (!video) return;
      video.play().catch(function(){});
      var fadeTimers = new WeakMap();
      function startFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        var t = setTimeout(function() { btn.classList.add('faded'); }, 1000);
        fadeTimers.set(btn, t);
      }
      function cancelFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        btn.classList.remove('faded');
      }
      document.addEventListener('click', function(e) {
        var btn = e.target.closest ? e.target.closest('.media-block .sound-btn') : null;
        if (!btn) return;
        e.preventDefault(); e.stopPropagation();
        var box = btn.closest('.media-block');
        if (!box) return;
        var vid = box.querySelector('.founder-video');
        if (!vid) return;
        var wasMuted = vid.muted;
        if (wasMuted) {
          vid.muted = false;
          btn.querySelector('.icon-play').style.display   = 'none';
          btn.querySelector('.icon-volume').style.display = '';
          startFade(btn);
        } else {
          vid.muted = true;
          btn.querySelector('.icon-play').style.display   = '';
          btn.querySelector('.icon-volume').style.display = 'none';
          cancelFade(btn);
        }
      }, true);
      document.addEventListener('mouseover', function(e) {
        var box = e.target.closest ? e.target.closest('.media-block') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        if (!btn) return;
        cancelFade(btn);
      }, true);
      document.addEventListener('mouseout', function(e) {
        var box = e.target.closest ? e.target.closest('.media-block') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        var vid = box.querySelector('.founder-video');
        if (!btn || !vid) return;
        if (!vid.muted) startFade(btn);
      }, true);
    })();
    /* ── COURSES CAROUSEL + FILTER ── */
    (function () {
      const track = document.getElementById('courseCarousel');
      const dotsWrap = document.getElementById('courseDots');
      if (!track || !dotsWrap) return;
      const allCards = Array.from(track.children);
      const perView = () => 2;
      const visibleCards = () => allCards.filter(c => c.style.display !== 'none');
      const pages = () => Math.max(1, Math.ceil(visibleCards().length / perView()));
      // Measure the exact scroll position of a card's left edge (accounts for
      // padding/gap precisely, using real rendered geometry — no peeking).
      function cardScrollLeft(card) {
        const trackRect = track.getBoundingClientRect();
        const cardRect = card.getBoundingClientRect();
        const paddingLeft = parseFloat(getComputedStyle(track).paddingLeft) || 0;
        return track.scrollLeft + (cardRect.left - trackRect.left) - paddingLeft;
      }
      function scrollToPage(i) {
        const visible = visibleCards();
        const card = visible[i * perView()];
        const target = card ? Math.max(0, cardScrollLeft(card)) : 0;
        track.scrollTo({ left: target, behavior: 'smooth' });
      }
      function currentPage() {
        const visible = visibleCards();
        let best = 0, bestDiff = Infinity;
        for (let i = 0; i < pages(); i++) {
          const card = visible[i * perView()];
          if (!card) continue;
          const diff = Math.abs(cardScrollLeft(card) - track.scrollLeft);
          if (diff < bestDiff) { bestDiff = diff; best = i; }
        }
        return best;
      }
      function buildDots() {
        dotsWrap.innerHTML = '';
        const n = pages();
        if (n <= 1) { dotsWrap.style.display = 'none'; return; }
        dotsWrap.style.display = 'flex';
        for (let i = 0; i < n; i++) {
          const d = document.createElement('button');
          d.className = 'dot';
          d.setAttribute('aria-label', 'Page ' + (i + 1));
          d.addEventListener('click', () => scrollToPage(i));
          dotsWrap.appendChild(d);
        }
        update();
      }
      function update() {
        const page = currentPage();
        Array.from(dotsWrap.children).forEach((d, i) => d.classList.toggle('active', i === page));
      }
      track.addEventListener('scroll', () => requestAnimationFrame(update));
      window.addEventListener('resize', buildDots);
      buildDots();

      let coursesAutoplayTimer = null;
      function startCoursesAutoplay() {
        stopCoursesAutoplay();
        coursesAutoplayTimer = setInterval(() => {
          const n = pages();
          if (n <= 1) return;
          const next = (currentPage() + 1) % n;
          scrollToPage(next);
        }, 4000);
      }
      function stopCoursesAutoplay() {
        if (coursesAutoplayTimer) clearInterval(coursesAutoplayTimer);
      }
      function restartCoursesAutoplay() { startCoursesAutoplay(); }
      startCoursesAutoplay();
      track.addEventListener('mouseenter', stopCoursesAutoplay);
      track.addEventListener('mouseleave', startCoursesAutoplay);

      document.querySelectorAll('.filter-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
          btn.classList.add('active');
          const filter = btn.dataset.filter || 'all';
          allCards.forEach(card => {
            const cats = (card.dataset.category || '').split(' ');
            const show = filter === 'all' || cats.includes(filter);
            card.style.display = show ? '' : 'none';
          });
          track.scrollTo({ left: 0, behavior: 'auto' });
          buildDots();
          restartCoursesAutoplay();
        });
      });
    })();
    /* ── MINI KITS SLIDER ── */
    (function () {
      const track   = document.getElementById('mkTrack');
      const dotsWrap = document.getElementById('mkDots');
      const btnPrev  = document.getElementById('mkPrev');
      const btnNext  = document.getElementById('mkNext');
      if (!track) return;
      const kits = Array.from(track.children);
      const total = kits.length;
      let current = 0;
      function buildDots() {
        if (!dotsWrap) return;
        dotsWrap.innerHTML = '';
        if (window.innerWidth > 600) { dotsWrap.style.display = 'none'; return; }
        dotsWrap.style.display = 'flex';
        kits.forEach((_, i) => {
          const d = document.createElement('button');
          d.className = 'dot' + (i === 0 ? ' active' : '');
          d.setAttribute('aria-label', 'Kit ' + (i + 1));
          d.addEventListener('click', () => goTo(i));
          dotsWrap.appendChild(d);
        });
      }
      function updateDots() {
        if (!dotsWrap) return;
        Array.from(dotsWrap.children).forEach((d, i) => d.classList.toggle('active', i === current));
      }
      function goTo(i) {
        current = ((i % total) + total) % total;
        const w = kits[0].getBoundingClientRect().width;
        track.style.transform = 'translateX(-' + (current * w) + 'px)';
        updateDots();
        btnPrev.disabled = false;
        btnNext.disabled = false;
      }
      if (btnPrev) btnPrev.addEventListener('click', () => goTo(current - 1));
      if (btnNext) btnNext.addEventListener('click', () => goTo(current + 1));
      window.addEventListener('resize', () => goTo(current));
      buildDots();
      goTo(0);
    })();
    /* ── TESTIMONIALS CAROUSEL ── */
    (function() {
      var viewport = document.getElementById('testViewport');
      var track    = document.getElementById('testGrid');
      var dotsWrap = document.getElementById('testDots');
      var btnPrev  = document.getElementById('testPrev');
      var btnNext  = document.getElementById('testNext');
      if (!viewport || !track) return;
      var current = 0;
      function getBoxes()  { return Array.from(track.children); }
      function visible()   { return window.innerWidth <= 600 ? 1 : window.innerWidth <= 900 ? 2 : 3; }
      function getGap()    { return 28; }
      function boxW()      { var vw = viewport.clientWidth; var n = visible(); return (vw - getGap() * (n - 1)) / n + getGap(); }
      function maxSlide()  { return Math.max(0, getBoxes().length - visible()); }
      function moveTo(i) {
        current = Math.max(0, Math.min(i, maxSlide()));
        track.style.transform = 'translateX(-' + (current * boxW()) + 'px)';
        if (dotsWrap) Array.from(dotsWrap.children).forEach(function(d,i){ d.classList.toggle('active', i===current); });
        if (btnPrev) btnPrev.disabled = current <= 0;
        if (btnNext) btnNext.disabled = current >= maxSlide();
      }
      function buildDots() {
        if (!dotsWrap) return;
        dotsWrap.innerHTML = '';
        var pages = maxSlide() + 1;
        if (pages <= 1) { dotsWrap.style.display = 'none'; return; }
        dotsWrap.style.display = 'flex';
        for (var i = 0; i < pages; i++) {
          (function(n){ var d = document.createElement('button'); d.className='dot'; d.addEventListener('click', function(){ moveTo(n); }); dotsWrap.appendChild(d); })(i);
        }
      }
      if (btnPrev) btnPrev.addEventListener('click', function(){ moveTo(current-1); });
      if (btnNext) btnNext.addEventListener('click', function(){ moveTo(current+1); });
      getBoxes().forEach(function(box) {
        var v = box.querySelector('.test-video');
        if (v) { v.muted = true; v.play().catch(function(){}); }
      });
      window.addEventListener('resize', function(){ buildDots(); moveTo(current); });
      buildDots(); moveTo(0);
    })();
    /* ── SOUND TOGGLE (testimonials) ── */
    (function() {
      var fadeTimers = new WeakMap();
      function startFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        var t = setTimeout(function() { btn.classList.add('faded'); }, 1000);
        fadeTimers.set(btn, t);
      }
      function cancelFade(btn) {
        clearTimeout(fadeTimers.get(btn));
        btn.classList.remove('faded');
      }
      document.addEventListener('click', function(e) {
        var btn = e.target.closest ? e.target.closest('.sound-btn') : null;
        if (!btn) return;
        e.preventDefault(); e.stopPropagation();
        var box = btn.closest('.test-box');
        if (!box) return;
        var vid = box.querySelector('.test-video');
        if (!vid) return;
        var wasMuted = vid.muted;
        document.querySelectorAll('.test-box').forEach(function(b) {
          var v2  = b.querySelector('.test-video');
          var b2  = b.querySelector('.sound-btn');
          if (!v2 || !b2) return;
          v2.muted = true;
          b2.querySelector('.icon-play').style.display   = '';
          b2.querySelector('.icon-volume').style.display = 'none';
          cancelFade(b2);
        });
        if (wasMuted) {
          vid.muted = false;
          btn.querySelector('.icon-play').style.display   = 'none';
          btn.querySelector('.icon-volume').style.display = '';
          startFade(btn);
        }
      }, true);
      document.addEventListener('mouseover', function(e) {
        var box = e.target.closest ? e.target.closest('.test-box') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        if (!btn) return;
        cancelFade(btn);
      }, true);
      document.addEventListener('mouseout', function(e) {
        var box = e.target.closest ? e.target.closest('.test-box') : null;
        if (!box) return;
        var btn = box.querySelector('.sound-btn');
        var vid = box.querySelector('.test-video');
        if (!btn || !vid) return;
        if (!vid.muted) startFade(btn);
      }, true);
    })();
    /* ── ANNOUNCEMENT BAR ── */
    (function () {
      var track = document.getElementById('topBarTrack');
      if (!track) return;
      var total = track.children.length;
      var real = total - 1;
      var index = 0;
      function goNext() {
        index++;
        track.style.transition = 'transform 0.6s ease';
        track.style.transform = 'translateX(-' + (index * 100) + '%)';
        if (index === real) {
          setTimeout(function () {
            track.style.transition = 'none';
            index = 0;
            track.style.transform = 'translateX(0%)';
          }, 620);
        }
      }
      setInterval(goNext, 2000);
    })();
    /* ── FIX: force course-card images to stay static ── */
    (function () {
      function fixCardImages() {
        document.querySelectorAll('.courses-section .card-image, .courses-section .card-image img')
          .forEach(function (el) {
            el.style.setProperty('position', 'static', 'important');
            el.style.removeProperty('top');
            el.style.removeProperty('left');
          });
      }
      fixCardImages();
      document.addEventListener('DOMContentLoaded', fixCardImages);
      var mo = new MutationObserver(fixCardImages);
      var target = document.querySelector('.courses-section') || document.body;
      mo.observe(target, { attributes: true, subtree: true, attributeFilter: ['style', 'class'] });
    })();
    /* ── INCLUDED CAROUSEL (course detail pages: "What all is included?") ── */
    (function () {
      const track = document.getElementById('includedCarousel');
      const wrap  = document.getElementById('includedDots');
      if (!track || !wrap) return;
      const cards = track.children.length;
      wrap.innerHTML = '';
      for (let i = 0; i < cards; i++) {
        const d = document.createElement('button');
        d.className = 'dot' + (i === 0 ? ' active' : '');
        d.setAttribute('aria-label', 'Item ' + (i + 1));
        d.addEventListener('click', () => { track.scrollTo({ left: (track.scrollWidth - track.clientWidth) * (i / (cards - 1)), behavior: 'smooth' }); });
        wrap.appendChild(d);
      }
      track.addEventListener('scroll', () => {
        const p = Math.round(track.scrollLeft / Math.max(1, track.scrollWidth - track.clientWidth) * (cards - 1));
        Array.from(wrap.children).forEach((d, i) => d.classList.toggle('active', i === p));
      });
    })();
    /* ── STUDENT GALLERY SLIDER (course detail pages): touch, drag, autoplay, dots ── */
    (function () {
      const viewport = document.getElementById('galleryViewport');
      const strip    = document.getElementById('galleryStrip');
      const dotsWrap = document.getElementById('galleryDots');
      if (!strip || !viewport) return;

      const photos      = Array.from(strip.children);
      const AUTOPLAY_MS = 5000;
      let current       = 0;
      let autoTimer     = null;

      function visibleCount() {
        const vw = viewport.clientWidth;
        if (vw <= 600)  return 1;
        if (vw <= 992)  return 2;
        return 4;
      }
      function totalPages() { return Math.max(1, photos.length - visibleCount() + 1); }
      function getSlideWidth() { return photos[0] ? photos[0].getBoundingClientRect().width + 3 : 0; }

      function goTo(idx) {
        const pages = totalPages();
        current = Math.max(0, Math.min(idx, pages - 1));
        const offset = current * getSlideWidth();
        strip.style.transform = `translateX(-${offset}px)`;
        updateDots();
      }

      function buildDots() {
        if (!dotsWrap) return;
        dotsWrap.innerHTML = '';
        const pages = totalPages();
        if (pages <= 1) { dotsWrap.style.display = 'none'; return; }
        dotsWrap.style.display = 'flex';
        for (let i = 0; i < pages; i++) {
          const d = document.createElement('button');
          d.className = 'dot' + (i === 0 ? ' active' : '');
          d.setAttribute('aria-label', 'Gallery page ' + (i + 1));
          d.addEventListener('click', () => { goTo(i); restartAutoplay(); });
          dotsWrap.appendChild(d);
        }
      }

      function updateDots() {
        if (!dotsWrap) return;
        Array.from(dotsWrap.children).forEach((d, i) => d.classList.toggle('active', i === current));
      }

      function startAutoplay() { autoTimer = setInterval(() => { goTo(current + 1 >= totalPages() ? 0 : current + 1); }, AUTOPLAY_MS); }
      function restartAutoplay() { clearInterval(autoTimer); startAutoplay(); }

      let startX = null, startLeft = 0, dragging = false;

      viewport.addEventListener('pointerdown', e => {
        startX    = e.clientX;
        startLeft = current * getSlideWidth();
        dragging  = false;
        strip.classList.add('is-dragging');
        clearInterval(autoTimer);
        viewport.setPointerCapture(e.pointerId);
      });
      viewport.addEventListener('pointermove', e => {
        if (startX === null) return;
        const dx = e.clientX - startX;
        if (Math.abs(dx) > 5) dragging = true;
        strip.style.transform = `translateX(-${startLeft - dx}px)`;
      });
      viewport.addEventListener('pointerup', e => {
        if (startX === null) return;
        strip.classList.remove('is-dragging');
        const dx = e.clientX - startX;
        if (dragging) {
          const threshold = getSlideWidth() * 0.2;
          if (dx < -threshold)      goTo(current + 1);
          else if (dx > threshold)  goTo(current - 1);
          else                      goTo(current);
        }
        startX = null; dragging = false; restartAutoplay();
      });
      viewport.addEventListener('pointercancel', () => {
        strip.classList.remove('is-dragging');
        goTo(current); startX = null; restartAutoplay();
      });
      viewport.addEventListener('mouseenter', () => clearInterval(autoTimer));
      viewport.addEventListener('mouseleave', restartAutoplay);
      window.addEventListener('resize', () => { buildDots(); goTo(Math.min(current, totalPages() - 1)); });

      buildDots();
      goTo(0);
      startAutoplay();
    })();
    /* ── ALL COURSES PAGE: course filter grid ── */
    (function () {
      var filterBar = document.getElementById('acCourseFilterCourses');
      var grid = document.getElementById('acCoursesGridCourses');
      var countLabel = document.getElementById('acCourseCountCourses');
      var noResults = document.getElementById('acNoResultsCourses');
      if (!filterBar || !grid) return;

      filterBar.addEventListener('click', function (e) {
        var btn = e.target.closest ? e.target.closest('.ac-filter-btn') : null;
        if (!btn || !filterBar.contains(btn)) return;

        e.preventDefault();
        e.stopPropagation();

        filterBar.querySelectorAll('.ac-filter-btn').forEach(function (b) {
          b.classList.remove('active');
        });
        btn.classList.add('active');

        var filter = btn.getAttribute('data-filter') || 'all';
        var cards = grid.querySelectorAll('.ac-course-card');
        var visible = 0;

        cards.forEach(function (card) {
          var cats = (card.getAttribute('data-category') || '').split(' ');
          var show = filter === 'all' || cats.indexOf(filter) !== -1;
          card.style.display = show ? '' : 'none';
          if (show) visible++;
        });

        if (countLabel) {
          countLabel.textContent = filter === 'all'
            ? 'Showing all ' + visible + ' workshops'
            : 'Showing ' + visible + ' workshop' + (visible === 1 ? '' : 's');
        }
        if (noResults) noResults.classList.toggle('show', visible === 0);
      });
    })();
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
